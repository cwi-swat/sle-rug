form testExample { 
    "hello"
        strtest: str
    
    "nice"
        inttest: integer
    
    "this is a boolean"
        booltest: boolean = true

    "changeBool"
        cBool: boolean        

    if ((inttest * inttest) > 100) {
        "What was the selling price?"
            sellingPrice: integer
    }  

    if(cBool && ((inttest *inttest) > 100)) {
        "hello there"
            helloThere: integer
    }

    if(cBool) {
        "wassup"
            wasSupp: integer
    }
}